info:
  name: create payments
  type: http
  seq: 10

http:
  method: POST
  url: POST http://localhost:3000/api/payments
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
