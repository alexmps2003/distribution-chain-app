info:
  name: create invoice
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:3000/api/invoices
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
